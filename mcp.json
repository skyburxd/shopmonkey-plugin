{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "grokbot-shopmonkey-plugin": {
      "type": "stdio",
      "command": "node",
      "args": ["./server.mjs"],
      "cwd": "${PLUGIN_ROOT}",
      "env": {
        "SM_TOKEN": "${SM_TOKEN}"
      }
    }
  }
}
